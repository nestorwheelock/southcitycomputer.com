# Optimized nginx config for monolithic Rust backend
# All static assets served from memory by Rust - nginx handles SSL/compression

server {
    listen 80;
    server_name southcitycomputer.com www.southcitycomputer.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name southcitycomputer.com www.southcitycomputer.com;

    # SSL will be configured by certbot
    # ssl_certificate /etc/letsencrypt/live/southcitycomputer.com/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/southcitycomputer.com/privkey.pem;

    # Maintenance page location
    root /root/southcitycomputer;

    # Custom error pages for backend unavailable
    error_page 502 503 504 /maintenance.html;
    location = /maintenance.html {
        internal;
        root /root/southcitycomputer;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Gzip compression (Rust sends uncompressed, nginx compresses)
    gzip on;
    gzip_vary on;
    gzip_min_length 1000;
    gzip_proxied any;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json application/xml image/svg+xml;
    gzip_comp_level 6;

    # Proxy all requests to Rust backend (serves everything from memory)
    location / {
        proxy_pass http://127.0.0.1:9000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Timeouts
        proxy_connect_timeout 5s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;

        # Buffer settings for fast responses
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;

        # Intercept errors to show maintenance page
        proxy_intercept_errors on;
    }

    # Longer cache for static assets (Rust sets cache headers too)
    location ~* \.(webp|jpg|jpeg|png|gif|ico|css|js|woff2|woff|ttf)$ {
        proxy_pass http://127.0.0.1:9000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        add_header Cache-Control "public, max-age=31536000, immutable";
        proxy_intercept_errors on;
    }
}
